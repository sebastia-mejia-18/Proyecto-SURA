// Hero.jsx

const Hero = () => {
    return (
        <section className="hero">
            <h2>Bienvenido a MiApp</h2>
            <p>Esta es la mejor aplicación para gestionar tus proyectos.</p>
        </section>
    );
};

export default Hero;  // Exportación por defecto
